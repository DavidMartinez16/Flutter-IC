import 'package:flutter/material.dart';

class MasPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Mas Page', textScaleFactor: 2.0),
      color: Colors.yellow,
    );
  }
}