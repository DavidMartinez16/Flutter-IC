import 'package:flutter/material.dart';

class UsuarioPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Usuario Page', textScaleFactor: 2.0),
      color: Colors.orange,
    );
  }
}