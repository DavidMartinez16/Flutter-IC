import 'package:flutter/material.dart';

class EncuestaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Encuesta Page', textScaleFactor: 2.0),
      color: Colors.red
    );
  }
}